{
  "name": "timeline",
  "version": "1.0.0",
  "description": "Claude Code session timeline extractor",
  "private": true,
  "engines": {
    "node": ">=24.15.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
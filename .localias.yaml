# Local development domain mapping
# Access the API at https://api.claude-dash.local
# Access the webapp at https://claude-dash.local (future)
#
# This file is committed to git for team sharing.
# Each developer needs localias installed: brew install peterldowns/tap/localias

api.claude-dash.local: 3001
claude-dash.local: 5173
